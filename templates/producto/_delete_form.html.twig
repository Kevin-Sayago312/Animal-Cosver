<form method="post" action="{{ path('app_producto_delete', {'id': resultado.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resultado.id) }}">
    <button class="btn">Eliminar</button>
</form>
